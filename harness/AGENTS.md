**IMPORTANT: Before responding to ANY user message, read `.workflow/active-role.md` first.** That file contains your specific assignment: what to do, what files to read, and what artifact to review or produce.

This session was launched via `workflow-role`, which prepared your assignment and placed it in `.workflow/active-role.md`. Follow the instructions in that file — it lists the exact files to read and the task to perform.

Do not use Claude Code skills (they are not available in this harness). Your instructions come from the files referenced in `.workflow/active-role.md`.
